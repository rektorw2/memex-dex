-- Режимы выхода PAPER-агента. Только добавление.
--
-- До этого позиция закрывалась одним способом — цена дошла до цели.
-- План выхода (стоп, ступени фиксации, трейлинг, выход по времени)
-- снимается при входе и хранится на самой позиции: смена режима на
-- счёте не переписывает правила уже открытых позиций.
--
-- Два поля JSON, а не десяток колонок: план версионирован внутри
-- себя (`version`), и добавление нового правила не требует новой
-- миграции. Старые строки читаются с NULL и ведутся как прежде —
-- режим TARGET.
--
-- Ни DROP, ни ALTER существующих колонок.

ALTER TABLE "PaperAgentAllocation" ADD COLUMN IF NOT EXISTS "exitPlan" JSONB;
ALTER TABLE "PaperAgentAllocation" ADD COLUMN IF NOT EXISTS "exitState" JSONB;

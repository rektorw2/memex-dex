import type { Config } from 'tailwindcss';

/**
 * Оформление торгового терминала.
 *
 * Палитра сдержанная намеренно: в интерфейсе, где человек принимает
 * денежные решения, цвет должен нести смысл, а не украшать. Поэтому
 * цветов ровно четыре — фиолетовый для выбора и действий, бирюзовый
 * для роста, красный для падения, жёлтый только для предупреждений.
 * Свечения и градиенты не используются: они мешают читать плотные
 * числовые таблицы.
 */
export default {
  content: ['./app/**/*.{ts,tsx}', './components/**/*.{ts,tsx}'],
  theme: {
    extend: {
      colors: {
        bg: '#080B0F',
        panel: '#11151B',
        /** Поверхность поверх панели: строки при наведении, вложенные блоки. */
        raised: '#171B22',
        border: '#252B35',
        white: '#F5F7FA',
        muted: '#8F98A7',
        accent: '#8B5CF6',
        up: '#22C7B8',
        down: '#FF5C6C',
        warn: '#E8B84A',
        /**
         * Оранжевый для высокого риска. Отдельно от красного намеренно:
         * между «высокий» и «критический» должна быть видимая разница,
         * иначе четырёхступенчатая шкала на глаз становится
         * трёхступенчатой.
         */
        riskHigh: '#FF8A3D',
      },
      fontFamily: {
        // Запасные шрифты подобраны по метрикам: если Inter не загрузится,
        // подставится системный интерфейсный шрифт, и вёрстка не поедет.
        sans: ['Inter', 'system-ui', '-apple-system', 'Segoe UI', 'sans-serif'],
        mono: ['JetBrains Mono', 'ui-monospace', 'SFMono-Regular', 'Menlo', 'monospace'],
      },
      borderRadius: {
        // Восьмёрка для элементов управления, двенадцать для панелей —
        // разница должна читаться, но не бросаться в глаза.
        DEFAULT: '8px',
        md: '8px',
        lg: '12px',
      },
      transitionDuration: {
        // Выдвижение панели навигации. В наборе Tailwind есть 200 и 300,
        // а класса duration-250 нет: он молча отбрасывается, и остаётся
        // значение по умолчанию — 150 мс. Панель при этом выезжает
        // заметно резче задуманного, и понять причину по коду нельзя,
        // потому что класс выглядит написанным.
        250: '250ms',
      },
      spacing: {
        // Высоты, на которые опирается сетка страницы.
        header: '60px',
        stats: '52px',
        row: '58px',
      },
    },
  },
  plugins: [],
} satisfies Config;

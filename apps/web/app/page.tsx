import { PlansPageContent } from './plans/page';

/**
 * Главная витрина продукта.
 *
 * Корень и `/plans` используют один компонент намеренно. Цены,
 * возможности и состояния кнопок приходят с сервера, поэтому копия
 * страницы однажды начала бы показывать другой тариф или устаревшее
 * состояние оплаты.
 */
export default function HomePage() {
  return <PlansPageContent />;
}

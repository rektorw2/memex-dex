import type { AuthStep } from './AuthShell';

/**
 * Шаги первого сценария — один список на все страницы.
 *
 * Индикатор прогресса врёт ровно тогда, когда каждая страница знает
 * свой набор шагов: где-то их четыре, где-то три, и человек видит,
 * как путь удлиняется под ним. Здесь список один и лежит рядом
 * с оболочкой, которая его рисует.
 *
 * Подписи короткие намеренно: на узком экране они стоят в одну
 * строку с точками.
 */
export const ONBOARDING_STEPS: readonly AuthStep[] = [
  { id: 'start', label: 'Начало' },
  { id: 'auth', label: 'Вход' },
  { id: 'verify', label: 'Почта' },
  { id: 'plans', label: 'Тариф' },
] as const;

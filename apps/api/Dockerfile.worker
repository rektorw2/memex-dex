# Воркеры используют тот же образ, но другую точку входа.
# Отдельный сервис нужен, чтобы масштабировать API и фоновые задачи
# независимо: рост числа пользователей и рост числа лимиток —
# это разные нагрузки.
FROM memex-api:latest
CMD ["node", "apps/api/dist/workers/index.js"]

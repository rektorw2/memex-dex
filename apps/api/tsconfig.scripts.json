{
  "//": "Скрипты не входят в сборку — их запускает tsx, а dist должен содержать только src. Но проверять их типы обязательно: скрипт, который не компилируется, обнаруживается в тот момент, когда им пытаются что-то проверить, то есть в самый неудачный.",
  "extends": "../../tsconfig.base.json",
  "compilerOptions": { "noEmit": true },
  "include": ["scripts/**/*.ts", "src/**/*.ts"]
}

# Логи проверки

Исходные `logs/*.log` сохранены локально и исключены общим gitignore. Для коммита точные копии упакованы в `logs.zip`; каждый файл архива побайтово сверен с оригиналом. Контрольные суммы — в `log-sha256.json`. Предыдущие логи не изменялись.

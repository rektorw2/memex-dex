# Проверка агента после публикации — 9 сентября 2026, 20:37–20:42 UTC

**Исправления опубликованы: сервер сообщает ревизию `2b0060e74764f517864ac3396487dc50d5e4da3e`, совпадающую с локальным HEAD. Агент работает, но своевременная доставка сигналов не обеспечена.** В проверенной выборке допустимого по сроку сигнала не было, поэтому новое реальное открытие позиции эта проверка не подтверждает.

## Сервер

Диагностика только чтением: публичный `/health/agent` и Prisma внутри `SET TRANSACTION READ ONLY` через Render Web Shell. Секреты не выводились. Начало окна 20:32:31 UTC приблизительно соответствует запуску процесса по uptime; конец среза 20:41:06 UTC.

По текущей baseline-стратегии найден **21 сигнал, все SKIPPED**. Все 21 пришли позже исходного 30-секундного срока, задержка **52 894–367 735 мс**. Окончательные решения: 18 `DECISION_DEADLINE_EXCEEDED`, 2 `AMOUNT_BELOW_THRESHOLD`, 1 `TOKEN_TOO_OLD`. Категория окончательного решения и факт поздней доставки — пересекающиеся показатели. Неизвестная дата пула у позднего сигнала сама по себе не доказывает отказ метаданных: запрос уже может быть не нужен из-за истёкшего срока.

Агент включён, baseline `okx-signal-v3-baseline`, распределение FIXED. Активные сессии ACTIVE и SHADOW: по $700 свободно, $0 в позициях, открытых позиций и дневных входов 0. Запрос по состояниям `PAPER_OPEN` и `PAPER_CLOSED` не вернул записей. На момент среза очередь metadata requests пуста, общий provider blockedUntil равен 0.

В обоих публичных снимках worker healthy, без последовательных ошибок прохода и ошибок обработки. Источник REST_ONLY, WS недоступен с кодом **60036**. Новый `restDelivery` честно показывает **BUDGET_UNCONFIRMED**, интервал 60 секунд, пять сетей, полный обход **300 секунд**, выделенный бюджет 0 и `timelyEntryGuaranteed=false`. Исправление опубликовано, но бюджет ускорения не настроен. Финальный снимок: 20:41:58 UTC, uptime 568 секунд, RSS 219 МБ. Эти короткие наблюдения не подтверждают устойчивость за сутки.

Доказательства: [начальный health](health-initial.json), [итоговый health](health-final.json), [сводка базы](production-summary.json). Сводка базы — транскрипция выбранных полей агрегированного результата, не полный дамп.

## Повторные тесты

| Проверка | Результат | Лог |
| --- | --- | --- |
| Штатный E2E на отдельной PostgreSQL 17 | **100/100**, 8 файлов | [e2e.log](logs/e2e.log) |
| Метаданные, лимиты, REST и состояние источника | **35/35**, 4 файла | [focused.log](logs/focused.log) |
| Регрессии наблюдателя | **12/12** | [observer.log](logs/observer.log) |
| git diff --check | PASS | Проверено после тестов |

Пройдены открытие → сопровождение → закрытие PAPER-позиции, защита от повторного открытия и движения капитала, настоящий worker + сервис метаданных + HTTP-адаптер с подменой внешнего ответа + PostgreSQL, ожидание слота и общий бюджет, 429, дедлайн, рестарт, ротация сетей без перекрытия запросов. Внешние сделки не выполнялись. E2E сам пересобрал core перед запуском.

Изолированный контейнер `memex-retest-2037-pg`, база `memex_retest_2037_e2e`, порт 127.0.0.1:55440. После тестов контейнер и его временные тома удалены; другие базы не изменялись. Итоги тестов подтверждены сохранёнными финальными сводками; после прерывания пользователем идентификаторы процессов уже были недоступны для повторного чтения кода выхода.

Полный набор 4420 тестов, типы и сборка ранее прошли на тех же исходниках, см. [предыдущий независимый отчёт](../agent-retest-20260909-1938/report.md). В этом прогоне повторены проверки поведения агента, без повторения всей сборки и UI-скриншотов. Отслеживаемые исходники не изменены.

## Что остаётся

Нужно восстановить WS с подтверждённой подпиской и настоящим событием либо подтвердить и выделить достаточный бюджет/RPS OKX для более частого REST. Интервал не менялся в ходе теста. Затем необходимы настоящий допустимый PAPER-вход → сопровождение → выход, наблюдение минимум 24 часа и подтверждение восстановления WS. Успешные локальные сценарии не заменяют эти проверки.

В этом прогоне не выполнялись коммит, push, деплой, изменения production-настроек и сделки. Порог $600, фильтр возраста, исходный дедлайн, semi-auto/auto, официальный OKX Signal, защита дублей и блокировка LIVE не менялись. Предыдущие доказательства сохранены.

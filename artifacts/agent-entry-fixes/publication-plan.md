# Публикация после независимого ревью — не выполнена

Текущая проверенная production-ревизия и точка возврата: `2b92c029ee9fad403afc09390d92761fa5f74ed6`. Этот план дополняет предыдущий, учитывая, что его три миграции уже есть в опубликованной версии. Новых миграций у этого кандидата нет.

1. Независимо проверить `review.patch`, список файлов и `source-manifest.json`. Затем отдельно разрешить commit/push/деплой. Не включать в коммит все накопленные чужие артефакты автоматически.
2. Перед публикацией перечитать фактическую ревизию, состояние PAPER, позиции, worker/source health и Render-план. Сохранить резервную копию базы. При необходимости приостановить только новые входы штатным контролем, оставив сопровождение. Эти production-действия сейчас не выполнялись.
3. Проверить, что все процессы используют общую production-базу и `PaperMetadataGate` существует. Запуск нового entrypoint выполняет штатный migrate deploy; не создавать второй механизм миграций и не стирать `_prisma_migrations`.
4. Опубликовать API/worker и web в согласованном окне. Свести пересечение старых и новых workers к минимуму: старый образ не соблюдает новый общий лимитер. План Render должен остаться 1c-2g. Пересобрать web с настоящим production `NEXT_PUBLIC_API_URL` и нужным basePath: локальный экспорт этой визуальной проверки использовал fixture API `127.0.0.1:4019` и не предназначен для публикации.
5. Сначала можно оставить ускорение REST выключенным: новые бюджетные поля по умолчанию 0. Проверить в `/health/agent` и UI `BUDGET_UNCONFIRMED`, продолжение медленного REST, срок настоящих сигналов, результаты metadata outcome и отсутствие залпов/повторных входов. Это не означает достижения готовности PAPER.
6. После подтверждения владельцем квоты настроить ускорение отдельным разрешённым изменением (см. ниже), затем проверить фактические промежутки между HTTP, roundMs, задержку получения каждого сигнала, 429 и расход. При `INSUFFICIENT_BUDGET` восстановить WS; не увеличивать срок сигнала и не ослаблять фильтры.
7. Подтвердить настоящий WS login → subscribe acknowledgement → настоящее событие. Затем дождаться допустимого PAPER-входа, сопровождения и выхода, сопоставить signalId со всеми включёнными strategyId. Запустить ранее подготовленный наблюдатель ≥24 ч; пропуски наблюдения или рестарты не считать PASS. LIVE/broadcast не включать.

## Что требуется владельцу от OKX

- В кабинете разработчика выбрать **тот проект и ключ, которые использует memex-api**, проверить действующую Market API подписку, остаток Premium и разрешение канала `dex-market-new-signal-openapi`. Отсутствующий `OKX_PLAN` — только отсутствие настройки приложения, не доказательство тарифа аккаунта.
- Уточнить разрешённый RPS для POST `/api/v6/dex/market/signal/list`, его область учёта и конкуренцию с остальными вызовами этого ключа. Выделить Signal долю месячного бюджета и долю RPS с учётом цен, иных Premium endpoint и других deployments.
- По 60036 передать поддержке OKX время отказа и имя канала, факт успешного login и отсутствия subscribe acknowledgement/event. Попросить подтвердить привязку подписки/доступ к каналу. Не передавать секрет, passphrase, подпись запросов или полный ключ в публичный чат. Здесь обращение в поддержку не отправлялось.
- Только после этих проверок согласовать значения: `OKX_PLAN`, `OKX_SIGNAL_REST_MONTHLY_BUDGET`, `OKX_SIGNAL_REST_REQUESTS_PER_SECOND`, `OKX_SIGNAL_REST_CONSUMERS`. Последнее включает потребителей одной выделенной доли, в том числе другие deployments; процессы на одной базе дополнительно делят общий слот. Приложение ограничивает выделение половиной опубликованной Premium-квоты плана.
- Иллюстрация, а не рекомендация без проверки аккаунта: Growth, 900000/месяц и 1 RPS для Signal, один потребитель → минимум 3 с между запросами, круг пяти сетей 15 с плюс фактическая задержка ответа. Это верхняя месячная оценка 892800 вызовов за 31 день, а не измеренный остаток OKX. Не покупать тариф, не менять ключи и не включать автоматическую оплату ради теста.

## Возврат без удаления данных

При регрессии сохранить журналы и вернуть согласованный образ `2b92c02` с новыми входами на паузе и сохранённым сопровождением открытых позиций. Не удалять Token, OkxSignal, PaperAgentRun, ledger, PaperMetadataGate и миграционную историю. Дополнительные JSON-поля совместимы с существующим форматом; очистка gate для ускорения обхода запретов недопустима. Возврат старого образа возвращает и прежние ограничения доставки/метаданных — это не способ обходить Retry-After. После повторной публикации нового образа сохранённые запреты/аренды должны учитываться до истечения срока.

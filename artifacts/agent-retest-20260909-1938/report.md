# Независимая повторная проверка PAPER-агента — 9 сентября 2026

Проверен текущий сервер только чтением и локальный кандидат из `artifacts/agent-entry-fixes/`. Локальные тесты прошли. На сервере новые исправления ещё не опубликованы, открытых или закрытых PAPER-run при проверке не обнаружено. Полная готовность PAPER не подтверждена.

## Текущий production

Срез Prisma в транзакции `SET TRANSACTION READ ONLY`: **19:39:59 UTC**. Ревизия сервера **2b92c029ee9fad403afc09390d92761fa5f74ed6**. Агент включён, baseline `okx-signal-v3-baseline`, режим распределения FIXED. У ACTIVE и SHADOW по $700 свободно, $0 в позициях, открытых позиций и дневных входов 0.

За последний час по baseline найдено **36 сигналов, все SKIPPED**:

| Окончательное решение | Количество |
| --- | ---: |
| AMOUNT_BELOW_THRESHOLD | 10 |
| DECISION_DEADLINE_EXCEEDED | 18 |
| TOKEN_TOO_OLD | 8 |

**30 из 36** доставлены позже 30 секунд. Это отдельный показатель задержки, он пересекается с категориями окончательных решений выше. Шесть сигналов пришли своевременно и имели сумму от $600: у Grok, HORST, DUOLIPA, SATS и SLAWTH отсутствовала дата пула, окончательное решение — истечение дедлайна; OTC отклонён по возрасту, дата пула 28 августа. Пять сигналов без даты нельзя считать полностью допустимыми входами: возраст не подтверждён.

Публичная проверка `/health/agent` в **19:38 UTC**: healthy, worker выполняет проходы, ошибок обработки и последовательных ошибок прохода 0. Источник **OK_REST_ONLY**, отказ WS с кодом **60036**. REST-интервал на сервере **60000 мс**. Значения плана, выделенного месячного бюджета и RPS для нового планировщика отсутствуют. Новая версия при этих значениях сохраняет консервативный интервал: публикация сама по себе не обеспечит быстрый опрос.

Доказательства: [production-summary.json](production-summary.json), [health-summary.json](health-summary.json). Это выборочные транскрипции отображённых результатов, не полные ответы или дамп базы. RSS около 1006 МБ, uptime около 18,3 часа: одиночный срез не доказывает утечку памяти или непрерывную исправную работу за сутки.

## Независимый локальный прогон

Основа кандидата — тот же HEAD плюс текущие изменения автора. Все **17 файлов** совпали с его SHA-256-манифестом до и после тестов: [до](source-check.json), [после](source-check-after.json). Исходники при проверке не изменялись.

| Проверка | Результат | Доказательство |
| --- | --- | --- |
| Core | 2072 теста, 91 файл, PASS | [unit-all.log](logs/unit-all.log) |
| API | 1892 теста, 122 файла, PASS | [unit-all.log](logs/unit-all.log) |
| Web | 456 тестов, 19 файлов, PASS | [unit-all.log](logs/unit-all.log) |
| Всего core/API/web | **4420 тестов**, PASS | [unit-all.log](logs/unit-all.log) |
| Штатный E2E | **100 тестов**, 8 файлов, PASS | [e2e-all.log](logs/e2e-all.log) |
| Регрессии наблюдателя | **12**, PASS | [observer.log](logs/observer.log) |
| Core build | PASS | [core-build.log](logs/core-build.log) |
| Типы API и web | PASS, exit 0 | [API](logs/api-types.log), [web](logs/web-types.log) |
| Полная сборка core/API/web, статические страницы | PASS, exit 0 | [build-all.log](logs/build-all.log) |
| git diff --check | PASS, exit 0 | [diff-check.log](logs/diff-check.log) |

E2E выполнялся на отдельной PostgreSQL 17: контейнер `memex-retest-1938-pg`, порт 127.0.0.1:55439, база `memex_retest_1938_e2e`. После завершения контейнер и его временные тома удалены. Другие контейнеры не изменялись. Ожидаемые сообщения Prisma о нарушении уникальности в E2E относятся к успешным отрицательным тестам дублей.

Подтверждены сценарии Robinhood worker → настоящий сервис метаданных → настоящий HTTP-адаптер с подменой внешнего ответа → PostgreSQL → **одно открытие PAPER**; отсутствие повторного открытия; недопустимый возраст и отсутствие даты; ожидание ёмкости; несколько токенов; 429/Retry-After; восстановление после рестарта и общий лимит через PostgreSQL. Пройдены тесты REST-ротации пяти сетей, отсутствия стартового всплеска и перекрытия запросов, ограничения бюджета, восстановления после 429. Штатный сценарий прибыльной PAPER-позиции открывает, сопровождает и закрывает её в изолированной базе.

Это подтверждение поведения на тестовых данных. Живой внешний Gecko и реальный допустимый production-сигнал в локальных тестах не использовались. Отчёт не заменяет полное ревью всех возможных конкурентных состояний и проверку реального рынка. Отдельная генерация Prisma и повторные скриншоты в этом прогоне не выполнялись.

## Публикация и оставшиеся проверки

Новых миграций кандидат не добавляет; использует дополнительные поля существующего JSON-состояния PaperMetadataGate. Существующий план публикации должен сохранять все три ранее согласованные миграции:

- `20260908130000_add_agent_live_wallet`;
- `20260909100000_add_worker_heartbeat`;
- `20260909120000_add_paper_metadata_gate`.

Публиковать нужно всю рабочую версию, включая новые пока неотслеживаемые файлы из манифеста. Перед изменением частоты REST нужны подтверждённые квоты и выделенный бюджет ключа OKX; альтернативно требуется восстановление WS с подтверждённой подпиской и настоящим событием. Наличие healthy и успешного REST-ответа не подтверждает своевременную доставку сигналов.

После публикации остаются настоящий допустимый PAPER-вход → сопровождение → выход, наблюдение минимум 24 часа и подтверждение восстановления WS. Исторические пропуски не переоткрывать. Порог $600, возраст и исходный дедлайн, правила semi-auto/auto, официальный OKX Signal, защита от дублей и блокировка LIVE не изменялись этой проверкой.

Коммит, push, деплой, изменение production-настроек и production-сделки не выполнялись. Предыдущие доказательства сохранены.

# Независимое ревью окончательных исправлений

Дата: 2026-09-09. Проверена незакоммиченная рабочая копия поверх 619e631. Новых блокирующих замечаний в проверенном изменении не найдено. Два дефекта предыдущего ревью закрыты.

## Выводы по исправлениям

- Ожидание метаданных больше не расходует попытку при занятой ёмкости. Worker повторно спрашивает допуск в рамках исходного срока; общий журнал PaperMetadataGate координирует процессы под PostgreSQL FOR UPDATE. Ограничены частота, число активных аренд и восстановлений. Завершение проверяет владельца попытки; дедлайн не обновляется при рестарте.
- Signal и Market имеют независимые состояния, защиту от перекрытия запросов и минутные повторы после ошибки. Успешный пустой список отличим от отсутствующего и истёкшего; часовой интервал применяется к действующему успешному подтверждению.
- Новая добавочная миграция `20260909120000_add_paper_metadata_gate` присутствует в каталоге загрузчика и применяется штатным Prisma migrate deploy. Таблица ограничена единственной строкой id=1.

## Независимые проверки

| Проверка | Итог |
| --- | --- |
| Core build и Prisma Client generate | PASS |
| Core | 2072/2072 |
| Полный API, включая миграционные проверки | 1866/1866 |
| Web | 455/455 |
| Всего core/API/web | **4393/4393** |
| Штатный E2E на отдельной PostgreSQL 17 | **94/94**, 7 файлов |
| Регрессии наблюдателя | **12/12** |
| TypeScript API | PASS |
| git diff --check | PASS |

E2E выполнен с новой локальной базой `memex_gate_e2e` на порту 55433. Включены настоящие worker/service/Prisma/DB проверки сериализации допуска, ожидания разных токенов, возобновления обработки и ограничения повторных попыток. Внешние данные заменены синтетическими; это не production-сделки. После прогона созданный контейнер `memex-gate-review-20260909` остановлен и удалён; другие контейнеры и базы не затрагивались. Логи сохранены рядом в `logs/`.

Прочитаны результаты контейнерного отката автора: новый образ → 619e631 → новый образ с одинаковым хешем данных и успешным стартом API. Повторно Docker-образы, откат, статическая сборка и UI в этом ревью не прогонялись. Репетиция автора с выключенными workers подтверждает сохранность данных и совместимость схемы; она не доказывает сопровождение новых сетевых позиций старым worker.

## Единственная правка ревью

В `artifacts/agent-production-followup/publication-plan.md` оставалась формулировка «две миграции». Исправлена на три с явными именами AgentLiveWallet, WorkerHeartbeat и PaperMetadataGate. Код приложения не менялся.

## Граница готовности

Локально изменение можно передавать на согласование коммита и публикации. Production в этом ревью не проверялся и не изменялся. Коммит, push и деплой не выполнялись.

После публикации остаются: актуальные Signal/Market/RPC-проверки трёх сетей, настоящий допустимый PAPER-вход → сопровождение → выход, непрерывное наблюдение ≥24 ч и успешная подписка плюс событие WS после решения доступа 60036. LIVE остаётся заблокированным. Успешный локальный набор тестов не заменяет эти проверки.
